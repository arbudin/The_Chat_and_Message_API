# The Chat and Message API

RESTful API для управления чатами и сообщениями, построенное на FastAPI с использованием PostgreSQL.

## 📋 Описание

API предоставляет функциональность для создания и управления чатами, а также отправки и получения сообщений. Проект использует современный стек технологий и готов к развертыванию через Docker.

## 🚀 Технологии

- **FastAPI** - современный веб-фреймворк для создания API
- **PostgreSQL** - реляционная база данных
- **SQLAlchemy** - ORM для работы с базой данных
- **Alembic** - система миграций базы данных
- **Pydantic** - валидация данных
- **Docker & Docker Compose** - контейнеризация приложения
- **Uvicorn** - ASGI сервер

## 📁 Структура проекта

```
The_Chat_and_Message_API/
├── alembic/              # Миграции базы данных
│   ├── versions/         # Файлы миграций
│   └── env.py           # Конфигурация Alembic
├── db/                   # Работа с базой данных
│   ├── database.py      # Подключение к БД
│   ├── models.py        # SQLAlchemy модели
│   └── logger.py        # Логирование
├── routers/              # API роутеры
│   ├── chats.py         # Эндпоинты для чатов
│   └── messages.py      # Эндпоинты для сообщений
├── schemas/              # Pydantic схемы
│   ├── chat.py          # Схемы для чатов
│   ├── message.py       # Схемы для сообщений
│   └── chat_with_messages.py
├── tests/                # Тесты
├── docker-compose.yml    # Конфигурация Docker Compose
├── Dockerfile           # Образ приложения
├── alembic.ini          # Конфигурация Alembic
├── main.py              # Точка входа приложения
└── requirements.txt     # Зависимости Python
```

## 🛠️ Требования

- Python 3.12+
- Docker и Docker Compose
- PostgreSQL 16 (если запускаете локально)

## 📦 Установка и запуск

### Вариант 1: Запуск через Docker Compose (Рекомендуется)

1. **Клонируйте репозиторий:**
   ```bash
   git clone <repository-url>
   cd The_Chat_and_Message_API
   ```

2. **Создайте файл `.env.docker`** (если его нет и вставьте данные вашей БД):
   ```env
   DB_USER=user
   DB_PASSWORD=pass
   DB_HOST=db
   DB_PORT=5432
   DB_NAME=db_name
   RUNNING_IN_DOCKER=true
   ```

3. **Запустите приложение:**
   ```bash
   docker compose up --build
   ```

   Приложение будет доступно по адресу: `http://localhost:8000`

### Вариант 2: Локальная установка

1. **Создайте виртуальное окружение:**
   ```bash
   python -m venv .venv
   .venv\Scripts\activate  # Windows
   # или
   source .venv/bin/activate  # Linux/Mac
   ```

2. **Установите зависимости:**
   ```bash
   pip install -r requirements.txt
   ```

3. **Создайте файл `.env`:**
   ```env
   DB_USER=postgres
   DB_PASSWORD=your_password
   DB_HOST=localhost
   DB_PORT=5432
   DB_NAME=chats_and_messages_db
   RUNNING_IN_DOCKER=false
   ```

4. **Настройте базу данных PostgreSQL** и создайте базу данных `chats_and_messages_db`

5. **Примените миграции:**
   ```bash
   alembic upgrade head
   ```

6. **Запустите приложение:**
   ```bash
   uvicorn main:app --reload
   ```

## 📚 API Документация

После запуска приложения доступна интерактивная документация:

- **Swagger UI**: `http://localhost:8000/docs`
- **ReDoc**: `http://localhost:8000/redoc`

## 🔌 API Эндпоинты

### Чаты (`/chats`)

#### Создать чат
```http
POST /chats/
Content-Type: application/json

{
  "title": "Название чата"
}
```

**Ответ:**
```json
{
  "id": 1,
  "title": "Название чата",
  "created_at": "2026-01-23T10:00:00"
}
```

#### Получить чат с сообщениями
```http
GET /chats/{chat_id}?limit=20
```

**Параметры:**
- `chat_id` (path) - ID чата
- `limit` (query, опционально) - количество сообщений (1-100, по умолчанию 20)

**Ответ:**
```json
{
  "id": 1,
  "title": "Название чата",
  "created_at": "2026-01-23T10:00:00",
  "messages": [
    {
      "id": 1,
      "chat_id": 1,
      "text": "Текст сообщения",
      "created_at": "2026-01-23T10:05:00"
    }
  ]
}
```

#### Добавить сообщение в чат
```http
POST /chats/{chat_id}/messages/
Content-Type: application/json

{
  "text": "Текст сообщения"
}
```

**Ответ:**
```json
{
  "id": 1,
  "chat_id": 1,
  "text": "Текст сообщения",
  "created_at": "2026-01-23T10:05:00"
}
```

#### Удалить чат
```http
DELETE /chats/{chat_id}
```

**Примечание:** При удалении чата все связанные сообщения удаляются каскадно.

### Сообщения (`/messages`)

#### Получить все сообщения
```http
GET /messages/
```

**Ответ:**
```json
[
  {
    "id": 1,
    "chat_id": 1,
    "text": "Текст сообщения",
    "created_at": "2026-01-23T10:05:00"
  }
]
```

#### Удалить сообщение
```http
DELETE /messages/{message_id}
```

## 🧪 Тестирование

Запуск тестов:
```bash
pytest
```
